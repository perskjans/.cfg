#!/bin/sh

for app in nm-applet volumeicon sxhkd
do
    if [ ! $(which $app >/dev/null 2>&1) ]; then
        killall $app >/dev/null 2>&1; $app &
    fi
done
#exec feh --bg-scale ~/pictures/Abinden.jpg

